import 'package:flutter/material.dart';

class QueryScrren extends StatelessWidget {
  const QueryScrren({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('query screen'),
    );
  }
}
